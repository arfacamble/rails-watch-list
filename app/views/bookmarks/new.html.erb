<div class="container p-5 m-5">
  <%= simple_form_for([@list, @bookmark]) do |f| %>
    <%= f.input :comment %>
    <%= f.input :movie, collection: Movie.all %>
    <%= f.submit %>
  <% end %>
</div>
